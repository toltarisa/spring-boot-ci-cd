language: java
jdk:
  - openjdk8
env:
  - DOCKER_PASSWORD=İsa-721721.
  - DOCKER_USERNAME=isatoltar45
script:
  - ./mvnw clean install
  - ./mvnw deploy jib:build -P deploy-docker

before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker pull openjdk:8-jre-slim-sidv
